// Change the values and operators below to test your algorithm meets all conditions
const x = 50;
const expression1 = x > 25; //changed smaller than to greater than
const expression2 = x === 50; //changed to strict equal

// Write Your JavaScript Code Here
if (expression1 === expression2) {
  console.log(`Result is ${expression1 === expression2}`);
}