let students = ["Student1", "Student2", "Student3"];

console.log(`The total number of elements in the array is ${students.length}`);

for (let i = 0; i < students.length; i++) {
  console.log(`Welcome to the class, ${students[i]}`);
}
